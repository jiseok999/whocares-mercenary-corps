# 조합(특성) 능력 설계 문서

> 목적: 현재 "모든 조합 = 공격력 +1"로 단조로운 시너지 시스템을, 조합마다 개성 있는
> 능력 + 색칸(조합 전용 칸) 메커니즘으로 확장하기 위한 기획 확정안.
>
> 방향: **전투 + 경제 균형**, 색칸은 **방식 A(시너지 보상) + 방식 B(라운드 색칸) 통합**.

---

## 1. 현재 시스템 요약 (코드 기준)

| 항목 | 내용 | 관련 코드 |
|------|------|-----------|
| 유닛 수 | 28종 (5등급~1등급) | `UnitCombatStats.BaseDamageByUnit` |
| 진화 | 1~4성, 배율 1.0 / 1.22 / 1.49 / 1.82 | `EvolutionDamageMultipliers` |
| 특성 | 종족·직업 8종 + 속성 7종 = 15종 | `UnitTraitData.unitTraits` |
| 현재 조합 효과 | 2/4/6/8단계마다 공격력 +1 (전 조합 동일) | `TraitManager.GetTierBonus` |
| 재화 | 골드(좀비 처치 시 +1) + 실버 코인 | `GameManager.AddGold / AddSilver` |
| 칸 버프(기존) | 보스 보상으로 특정 칸 점등 → 공격력/공속 +10% | `GameManager.BossTileBuffType` 외 |

### 특성별 유닛 분포

| 계열 | 특성 | 보유 유닛 수 | 유닛 번호 |
|------|------|------|-----------|
| 종족/직업 | 도깨비 | 6 | 1, 5, 9, 16, 17, 27 |
| 종족/직업 | 마법사 | 5 | 2, 7, 15, 19, 22 |
| 종족/직업 | 기사단 | 4 | 4, 10, 12, 21 |
| 종족/직업 | 재앙 | 4 | 14, 23, 25, 28 |
| 종족/직업 | 악마 | 3 | 8, 11, 20 |
| 종족/직업 | 파수꾼 | 3 | 3, 18, 24 |
| 종족/직업 | 도적단 | 2 | 6, 13 |
| 종족/직업 | 펭귄 | 1 | 26 |
| 속성 | 자연 | 5 | 1, 4, 8, 18, 22 |
| 속성 | 얼음 | 5 | 6, 11, 15, 25, 26 |
| 속성 | 불 | 4 | 2, 13, 16, 23 |
| 속성 | 번개 | 4 | 3, 5, 12, 20 |
| 속성 | 바람 | 4 | 7, 10, 24, 27 |
| 속성 | 빛 | 3 | 9, 14, 21 |
| 속성 | 어둠 | 3 | 17, 19, 28 |

---

## 2. 색칸(조합 전용 칸) 통합 설계

당신 핵심 아이디어("특정 칸에 색이 들어오고, 그 칸에 유닛을 올리면 추가 효과")를
두 레이어로 통합한다. 둘 다 기존 `BoardCell.SetBuffIndicators()` 색칠·아이콘 코드를 재사용.

### 레이어 A — 시너지 색칸 (조합 달성 보상, 영구)
- 조합이 **특정 단계에 도달하면**, 그 조합 전용 색칸이 보드에 점등된다.
- 그 칸에 **같은 조합 유닛**을 올리면 → 조합 고유 효과가 **강화(2배 또는 추가 보너스)**.
- 다른 조합 유닛을 올려도 → 약한 공용 버프(공격력 +5%).
- 예: 도깨비 4단계 → 코너 칸이 초록으로 점등 → 그 칸의 도깨비 유닛은 골드 효과 2배.

### 레이어 B — 라운드 색칸 (매 스테이지 랜덤, 일시)
- 스테이지 시작 시 보드의 1~2칸이 **랜덤 속성 색**으로 점등.
- 같은 속성 유닛을 올리면 → 강한 버프 + **라운드 종료 시 골드 +2**.
- 아무 유닛이나 올리면 → 약한 기본 버프(공격력 +5%)만.
- 매 스테이지 위치/속성이 바뀌어 **배치 퍼즐성** 부여.

### 우선순위/중첩 규칙
- 한 칸에 A·B가 겹치면 효과는 **합산**, 단 같은 종류 버프(% 공격력 등)는 **가장 큰 값만 적용**(난수 폭주 방지).
- 색 표시는 기존처럼 색을 평균내고 아이콘을 나열(`SetBuffIndicators`의 mixed 컬러 방식 그대로).

---

## 3. 종족/직업 조합 능력 (확정안)

> 숫자는 1차 제안값. 밸런스 패스에서 조정 가능. %는 곱연산, 정수는 합연산.

| 조합 | 단계 | 능력 | 색칸 강화(레이어 A) |
|------|------|------|---------------------|
| **도깨비** | 2 / 4 / 6 | 라운드 종료 시 골드 **+1 / +2 / +4** (경제 핵심) | 도깨비 칸의 도깨비 유닛 → 골드 효과 2배 |
| **마법사** | 2 / 4 | 스킬·투사체 데미지 **+15% / +35%** | 마법사 칸 → 스킬 재사용 -15% |
| **기사단** | 2 / 4 | 기사단 유닛 보호막 **+15% / +30%** 최대체력, 받는 피해 -10% | 기사단 칸 → 보호막 추가 +10% |
| **재앙** | 2 / 4 | 적 처치 시 누적 스택, 스택당 전체 공격력 +1% (최대 +20%) | 재앙 칸 → 처치 시 소폭 광역 폭발 |
| **악마** | 2 / 3 | 공격의 **8% / 15% 흡혈** + 적 최대체력 비례 추가 피해 | 악마 칸 → 흡혈 2배 |
| **파수꾼** | 2 / 3 | 사거리 **+20% / +35%** | 파수꾼 칸 → 첫 타 치명타 |
| **도적단** | 2 | 적 처치 시 **20% 확률 골드 +1 또는 실버 +1** | 도적단 칸 → 드랍 확률 2배 |
| **펭귄** | 1 (유니크) | 펭귄 인접 8칸 유닛 공속 **+10%** | 펭귄 칸 → 인접 버프 +20%로 강화 |

---

## 4. 속성 조합 능력 (확정안)

| 조합 | 단계 | 능력 | 비고 |
|------|------|------|------|
| **불** | 2 / 4 | 화상: 공격 시 대상에 초당 공격력의 **10% / 20%** 도트딜(3초) | 지속 피해 |
| **얼음** | 2 / 4 | 적 이동속도 **-15% / -25%**, 4단계 시 10% 빙결(1초) | CC형 |
| **자연** | 2 / 4 | 라운드 사이 배리어 회복 **+5% / +10%** + 라운드 종료 골드 **+1 / +2** | 경제+생존 |
| **번개** | 2 / 4 | 공격이 인접 적 **1 / 2명**에게 연쇄(원본 50% 피해) | 광역 |
| **바람** | 2 / 4 | 전체 유닛 공속 **+10% / +20%** | 단순 강력 |
| **빛** | 2 / 3 | 아군 전체 공격력 **+8% / +15%** | 글로벌 버프 |
| **어둠** | 2 / 3 | 적 방어력 **-15% / -25%**(받는 피해 증가) | 디버프 |

---

## 5. 구현 매핑 (기존 코드 연결점)

| 설계 요소 | 연결할 기존 코드 | 작업 내용 |
|-----------|------------------|-----------|
| 조합별 차등 효과 | `TraitManager.GetTierBonus`, `Recalculate` | 조합별 효과 테이블로 교체, 효과 타입(데미지/공속/골드/CC) 분리 |
| 데미지 보너스 적용 | `UnitCombatStats.CalculateDisplayDamage` → `TraitManager.GetDamageBonusForUnit` | 곱연산 버프 추가 경로 필요 |
| 공속/CC/흡혈 | `Character` (공격 루프) | 조합 효과를 읽어 공격 쿨다운·온히트 효과 적용 |
| 골드 효과 | `GameManager.AddGold`, 라운드 종료 훅 | 라운드 종료 시 조합 골드 정산 호출 추가 |
| 색칸 표시 | `BoardCell.SetBuffIndicators`, `GameManager` 버프칸 갱신 루프 | 레이어 A/B 칸 계산 로직 추가 |
| 라운드 색칸 갱신 | `RoundWaveController` 라운드 시작 시점 | 랜덤 칸/속성 선정 → 색칸 등록 |

---

## 6. 밸런스 노트 (1차 가이드)

- **경제 조합(도깨비·자연·도적단)** 은 즉시 전투력은 낮지만 후반 스노우볼 → 의도된 트레이드오프.
- **% 곱연산 버프 상한**: 단일 유닛 합산 +60% 이내 권장(바람20 + 빛15 + 색칸 등 누적 폭주 방지).
- **펭귄(1종)** 은 유일한 단일 시너지 → 희귀 유닛으로 등급/등장률 별도 관리 필요.
- 색칸 레이어 B의 라운드 골드는 **스테이지당 최대 +4** 정도로 캡.

---

## 6.5 구현 상태 (1차 적용 완료)

> 코드에 실제 반영된 내용. `TraitManager.traitEffects` 테이블이 단일 소스.

| 효과 채널 | 적용 조합 | 상태 |
|-----------|-----------|------|
| 공격력 배율(%) | 마법사·빛·어둠 | ✅ `Character.GetDamageWithUpgrade` + 표시값(`UnitCombatStats`) |
| 공격속도 배율(%) | 바람 (+펭귄 인접 오라) | ✅ `Character.TryAttack` |
| 공격력 누적 스택 | 재앙(처치당 +1%/+2%, 상한 +15%/+30%) | ✅ `TraitManager.CalamityStack` |
| 라운드 종료 골드 | 도깨비(+1/2/4), 자연(+1/2) | ✅ `GameManager.GrantTraitRoundEndGold` |
| 처치 시 확률 골드 | 도적단(20%) | ✅ `GameManager.OnZombieKilled` |
| 조합 패널 효과 표시 | 전체 | ✅ 행에 마우스 호버 시 툴팁(`TraitManager.UpdateTraitTooltip`)으로 단계별 효과 표시 |

**색칸 레이어 A (조합 시너지 칸) — ✅ 구현 완료**

> 처음 시도한 레이어 B(매 라운드 랜덤 색칸)는 "조합이 없어도 칸이 떠서 헷갈린다"는 피드백으로
> 레이어 A(조합 발동 시에만 점등)로 교체함.

| 요소 | 내용 | 코드 |
|------|------|------|
| 조합 발동 시 칸 점등 | 조합이 단계 1 이상 발동되면 그 조합 **전용 칸**이 점등(조합 없으면 안 뜸) | `GameManager.RefreshSynergyCells` |
| 조합→칸 매핑 | `SynergyCellDefs`에 등록된 5개 조합만 칸 배정 | `GetSynergyCellIndex` |
| 색/아이콘 표시 | 조합별 색 + `Ability_xxx` 아이콘, 보스 버프칸과 합성 | `GetTraitCellColor/GetTraitCellIcon` |
| 같은 조합 유닛 배치 | **해당 조합 효과 2배** (조합별 상세) | `IsUnitOnMatchingSynergyCell`, `TraitManager`/`Character`/`Barrier` |
| 활성 조합 노출 | `TraitManager.GetActiveTraits()` | `TraitManager.Recalculate` |
| 변경 시에만 갱신 | 활성 조합 시그니처 비교로 불필요한 재계산 방지 | `lastSynergySignature` |
| 툴팁 | 칸 호버 시 조합/효과 표시 | `GetBoardCellBuffInfos` |

**고유 온히트 메커니즘 — ✅ 구현 완료 (얼음·불·번개)**

> 투사체 20종을 건드리지 않고 `Character.TryAttack`의 타겟 해결 지점에서
> `ApplyComboOnHit(target)`로 발사 유닛의 속성 조합 효과를 적용.

| 조합 | 단계 | 메커니즘 | 코드 |
|------|------|----------|------|
| 얼음 | 2 / 4 | 명중 시 감속(1.5s) / 감속(2s)+25% 빙결(1s) | `Zombie.ApplySlow/ApplyUnit11Freeze` |
| 불 | 2 / 4 | 명중 시 화상 DoT(공격력 10%/20% per초, 3초) | `Zombie.ApplyBurn` + `UpdateBurn` |
| 번개 | 2 / 4 | 명중 시 인접 1체/2체 연쇄(50% 피해) | `Character.ApplyChainDamage` |
| 파수꾼 | 2 / 3 | **각 좀비 첫 명중**에만 치명타 추가 피해(+50% / +100%) | `Zombie.TryConsumeWardenCrit` + `Character.ApplyComboOnHit` |
| 악마 | 2 / 3 | **처형**: 체력 12% / 18% 이하 일반 좀비 즉시 처치(보스 제외) | `Zombie.IsBossLike` + `Character.ApplyComboOnHit` |

- 효과 타입을 `TraitEffectKind.OnHitEffect`로 분리 → 데미지 배율과 **중복 적용 안 됨**.
- 툴팁/패널 설명은 `levelDescriptions`로 실제 메커니즘을 단계별로 표기.

**고유 메커니즘 — ✅ 구현 완료 (재앙·펭귄)**

| 조합 | 단계 | 메커니즘 | 코드 |
|------|------|----------|------|
| 재앙 | 2 / 4 | 적 처치마다 재앙 유닛 공격력 +1% / +2% (단계별 상한 +15% / +30%, 라운드마다 초기화) | `TraitManager.AddCalamityKillStack` + `CalamityStack` 배율, `GameManager.OnZombieKilled`/`BeginRound` |
| 펭귄 | 1 | 펭귄 유닛 **인접 8칸** 유닛 공격속도 +15% (자신 제외) | `TraitManager.ApplyPenguinAura` (`AdjacencyAttackSpeed`) |

- 재앙: `TraitEffectKind.CalamityStack` — `calamityStacks`를 매 처치 누적, `Recalculate`에서 재앙 유닛 공격력에 `min(스택×단계증가, 상한)` 적용. 라운드 시작 시 `ResetCalamityStacks`로 초기화. 툴팁에 **현재 스택/적용 보너스** 실시간 표기.
- 펭귄: `TraitEffectKind.AdjacencyAttackSpeed` — 펭귄 칸 좌표를 모아 인접 칸 유닛의 공속 배율에 곱연산. 본인은 제외.

**벽(Barrier) 연동 메커니즘 — ✅ 구현 완료 (기사단)**

> 이 게임의 유일한 방어 대상인 오른쪽 벽(`Barrier`, HP 0 → 게임오버)에 관여하는 첫 조합.

| 조합 | 단계 | 메커니즘 | 코드 |
|------|------|----------|------|
| 기사단 | 2 / 4 | 벽 받는 피해 -15% / -30% + 라운드 시작 시 벽 +15 / +30 회복 | `TraitManager.BarrierGuard`, `Barrier.TakeDamage/Heal`, `GameManager.BeginRound` |

- `TraitEffectKind.BarrierGuard` — 전역 효과로 집계(`barrierDamageReduction`, `barrierRoundHeal`).
- `Barrier.TakeDamage`에서 감소율 적용, `GameManager.BeginRound`에서 `Barrier.Heal` 호출(최대치 초과 불가).

> 악마: 유닛 피격 구조가 없어 흡혈 불가 → **처형(체력 비율 즉사, 보스 제외)** 으로 채택.
> 기사단: 유닛 보호막 대신 **벽 수호(피해 감소 + 라운드 회복)** 로 채택.
> 파수꾼: 타겟팅이 "거리 무제한 최근접"이라 문자 그대로의 사거리 버프는 무의미 →
> 설계의 보조 효과였던 **첫 타 치명타**를 고유 메커니즘으로 채택(좀비별 1회, 어느 파수꾼이든 첫 명중이 치명타).

**시너지 칸 2배 보너스 — ✅ 구현 완료**

| 조합 | 시너지 칸 보너스 |
|------|------------------|
| 도깨비 | 라운드 종료 골드 2배 |
| 마법사 | 공격력 보너스 2배 (해당 유닛) |
| 기사단 | 벽 피해 감소·회복 2배 |
| 재앙 | 광란 스택 공격력 보너스 2배 (해당 유닛) |
| 악마 | 처형 임계치 2배 (12→24%, 18→36%) |

---

## 7. 미정 / 다음 논의 사항

- [x] 특성별 유닛 분포는 `UnitTraitData`로 재검증 완료(불=4종으로 정정).
- [ ] 색칸 A의 "조합별 지정 칸 위치"를 어떻게 정할지(고정 패턴 vs 보스 보상처럼 선택).
- [ ] 흡혈·보호막 등 신규 스탯이 `Character`에 없으면 필드 추가 필요.
- [ ] UI: 조합 패널(`TraitManager.UpdateTraitUI`)에 효과 설명 툴팁 추가 여부.
